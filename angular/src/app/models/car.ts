import { CarRegister } from './carRegister';

export class Car {
    id: number;
    brand: string;
    mode: string;
    year: number;
    registeredNumber: string;
    mileage: number;
    carType: string;
    carRegister: CarRegister
}