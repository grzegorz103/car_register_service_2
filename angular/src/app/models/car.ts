import { CarType } from './carType';

export class Car {
    id: number;
    brand: string;
    mode: string;
    year: number;
    registeredNumber: string;
    mileage: number;
    carType: CarType;
}